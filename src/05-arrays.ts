const superHeros: string[] = [];
const heroPower: number[] = [];
// or you can also do this
const heroSPower: Array<number> = [];

type Users1 = {
	name: string;
	isActive: boolean;
};

const allUsers: Users1[] = [];

superHeros.push('Gang');
heroPower.push(4);

allUsers.push({ name: 'mnk', isActive: true });

// Two Dimensional arrays
const MLModels: number[][] = [[255, 255, 255], []];
